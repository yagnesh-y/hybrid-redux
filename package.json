{
  "name": "hybrid-shared-redux",
  "version": "1.0.4",
  "description": "shared redux for web and mobile apps",
  "main": "dist",
  "scripts": {
    "build": "babel src -d dist",
    "prepublish": "rm -rf dist && npm run build"
  },
  "author": "Yagnesh",
  "license": "ISC",
  "dependencies": {
    "redux": "^4.0.5",
    "redux-thunk": "^2.3.0"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-env": "^1.7.0",
    "babel-preset-stage-0": "^6.24.1",
    "redux-devtools-extension": "^2.13.8",
    "redux-logger": "^3.0.6"
  }
}
